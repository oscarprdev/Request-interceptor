import { Request, Response } from 'express';

export interface IController {
  // This is a generic interface that all controllers will implement
  // Each controller will have its own specific methods
} 